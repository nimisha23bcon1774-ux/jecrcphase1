//Problem 4-termary operator
//find the greater number among 2
#include <iostream>
using namespace std;

int main()
{
    int a,b;
    int max_number;
    
    cin >> a >> b;
    //Termary operator
    max_number = (a>b) ? a : b;
    
    cout << max_number;
    return 0;
}
