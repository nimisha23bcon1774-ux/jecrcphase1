//Problem 2- If-else condition
//to verify user's age whether eligible for vote or not?
#include<iostream>
using namespace std;

int main()
{
  int age;
  cin >> age;
  
  if(age>=18)
  {
      cout << "ELIGIBLE";
  }
  else
  {
      cout << "NOT ELIGIBLE";
  }
  return 0;
}
